pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.5.0'
}

rootProject.name = 'shmtu-cas-kotlin'

include 'cas_lib'
include 'cas_cli'
include 'cas_android_lib'
